<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ZiaMonitoring_App.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:ZiaMonitoring_App.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24,16,24,16" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="A propos" Foreground="{StaticResource ZiaTextBrush}" />

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel Spacing="8">
                <TextBlock Text="Zia Monitoring" Style="{StaticResource CardTitleTextStyle}" />
                <TextBlock x:Name="VersionLabel" Foreground="{StaticResource ZiaVioletBrush}" />
                <TextBlock Text="Monitoring PC temps reel, jeux, securite et optimisation — open source (MIT)."
                           Foreground="{StaticResource ZiaTextBrush}" TextWrapping="Wrap" />
                <HyperlinkButton Content="Code source sur GitHub" NavigateUri="https://github.com/Aymezia/Zia-Monitoring" />
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardStyle}">
            <StackPanel Spacing="10">
                <TextBlock Text="Mises a jour" Style="{StaticResource CardTitleTextStyle}" />
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <Button x:Name="CheckUpdateButton" Content="Verifier les mises a jour" Click="CheckUpdate_Click" />
                    <Button x:Name="OpenReleaseButton" Content="Ouvrir la page de telechargement"
                            Visibility="Collapsed" Click="OpenRelease_Click" />
                </StackPanel>
                <TextBlock x:Name="UpdateStatusLabel" Foreground="{StaticResource ZiaTextBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</Page>
