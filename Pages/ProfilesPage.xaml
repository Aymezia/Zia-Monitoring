<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ZiaMonitoring_App.Pages.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    mc:Ignorable="d"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006">

    <ScrollViewer>
        <Grid Padding="24" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Profils d'optimisation" Style="{StaticResource TitleTextBlockStyle}" Foreground="{StaticResource ZiaTextBrush}" />
                    <TextBlock Text="Appliquez un profil configure en un clic." Foreground="{StaticResource ZiaMutedBrush}" />
                    <TextBlock x:Name="ProfileIoStatusLabel" Foreground="{StaticResource ZiaVioletBrush}" TextWrapping="Wrap" />
                </StackPanel>
                <Button Grid.Column="1" x:Name="ExportProfilesButton" Content="Exporter" Click="ExportProfiles_Click" VerticalAlignment="Center" />
                <Button Grid.Column="2" x:Name="ImportProfilesButton" Content="Importer" Click="ImportProfiles_Click" VerticalAlignment="Center" />
            </Grid>

            <ListView Grid.Row="1" x:Name="ProfilesList" MaxHeight="400">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
                            <Grid ColumnSpacing="16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="17" Foreground="{StaticResource ZiaTextBrush}" />
                                    <TextBlock Text="{Binding Description}" Foreground="{StaticResource ZiaMutedBrush}" TextWrapping="Wrap" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="Appliquer" Tag="{Binding Name}" Click="ApplyProfile_Click" Style="{StaticResource AccentButtonStyle}" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="Cache navigateurs" Style="{StaticResource CardTitleTextStyle}" />
                    <Button x:Name="EstimateCacheButton" Content="Estimer la taille des caches" Click="EstimateCache_Click" />
                    <TextBlock x:Name="CacheSizeLabel" Foreground="{StaticResource ZiaMutedBrush}" TextWrapping="Wrap" />
                    <Button x:Name="CleanCacheButton" Content="Nettoyer les caches (Chrome / Edge / Firefox)" Click="CleanCache_Click" Style="{StaticResource AccentButtonStyle}" />
                    <TextBlock x:Name="CacheCleanLabel" Foreground="{StaticResource ZiaMutedBrush}" TextWrapping="Wrap" />
                    <TextBlock Text="Nettoyage profond" Style="{StaticResource CardTitleTextStyle}" Margin="0,10,0,0" />
                    <TextBlock Text="Temp utilisateur et Windows + shader caches GPU (NVIDIA / AMD / Intel / DirectX). Planifiable chaque nuit dans les Paramètres." Style="{StaticResource CaptionTextStyle}" TextWrapping="Wrap" />
                    <Button x:Name="DeepCleanButton" Content="Lancer le nettoyage profond" Click="DeepClean_Click" Style="{StaticResource AccentButtonStyle}" />
                    <TextBlock x:Name="DeepCleanLabel" Foreground="{StaticResource ZiaMutedBrush}" TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </Grid>
    </ScrollViewer>
</Page>
